spring:
  datasource:
    url: jdbc:mysql://${USER:hackme}:${PASS:hackme}@${HOST:localhost}:${PORT:3306}/${DB:hackme}
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update